<footer class="p-3">
    <div class="row">
        <div class="col-12">
            <div class="nav flex-column">
                <div class="row">
                    {{#each data}}
                    {{#each this}}
                    <div class="col-3">
                        <div class="nav-item">
                            <a class="nav-link {{#if @last}}disabled{{/if}}" href="{{href}}">{{text}}</a>
                        </div>

                    </div>
                    {{/each}}
                    {{/each}}
                </div>
            </div>
        </div>
    </div>

</footer>