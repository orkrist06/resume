<header class="p-3">
    <div class="row">
        <div class="card">
            <div class="nav">
                {{#each data}}
                <div class="nav-item">
                    <a class="nav-link" href="{{href}}">{{text}}</a>
                </div>
                {{/each}}
            </div>
        </div>
    </div>
</header>